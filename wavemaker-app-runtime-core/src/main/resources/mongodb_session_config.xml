<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           		http://www.springframework.org/schema/data/mongo
           		http://www.springframework.org/schema/data/mongo/spring-mongo-3.0.xsd">

    <bean id="mongoCredential" class="com.mongodb.MongoCredential">
        <constructor-arg type="com.mongodb.AuthenticationMechanism" name="mechanism" value="SCRAM_SHA_1"/>
        <constructor-arg type="java.lang.String" name="userName" value="${security.session.mongodb.username}"/>
        <constructor-arg type="java.lang.String" name="source" value="${security.session.mongodb.dbname}"/>
        <constructor-arg type="char[]" name="password" value="${security.session.mongodb.password}"/>
    </bean>

    <bean id="credentialsList" class="java.util.ArrayList">
        <constructor-arg>
            <list>
                <ref bean="mongoCredential"/>
            </list>
        </constructor-arg>
    </bean>

    <bean id="mongoClient" class="org.springframework.data.mongodb.core.MongoClientFactoryBean">
        <property name="credential" ref="credentialsList"/>
        <property name="port" value="${security.session.mongodb.port}"/>
        <property name="host" value="${security.session.mongodb.host}"/>
    </bean>

    <mongo:db-factory dbname="${security.session.mongodb.dbname}" mongo-client-ref="mongoClient" id="mongoDbFactory" />
    <mongo:template id="mongoTemplate" db-factory-ref="mongoDbFactory" />

    <bean id="sessionRepository" class="org.springframework.session.data.mongo.MongoIndexedSessionRepository">
        <constructor-arg ref="mongoTemplate"/>
        <property name="maxInactiveIntervalInSeconds" value="#{${security.general.session.timeout} * 60}"/>
    </bean>

    <bean id="sessionRegistry" class="org.springframework.session.security.SpringSessionBackedSessionRegistry">
        <constructor-arg ref="sessionRepository"/>
    </bean>

</beans>